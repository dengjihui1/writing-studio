interface:
  display_name: "Writing Studio"
  short_description: "多体裁写作、深度润色与自然作者声音工作流"
  default_prompt: "Use $writing-studio to improve this writing at the edit depth I request, preserving its evidence, protected text, content budget, claim strength, and authentic voice."
policy:
  allow_implicit_invocation: true


